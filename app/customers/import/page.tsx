import {AppShell} from "@/components/AppShell";import {requireRouteAccess} from "@/lib/auth/session";import {requireCustomerPermission} from "@/lib/customers/permissions";import {uploadImportAction} from "../actions";
export default async function ImportPage({searchParams}:{searchParams:Promise<{error?:string}>}){const s=await requireRouteAccess("/customers");requireCustomerPermission(s.role,"IMPORT_CUSTOMERS");const p=await searchParams;return <AppShell active="/customers" title="Импорт клиентов" subtitle="CSV или XLSX · preview перед записью">{p.error&&<p className="notice error">{p.error}</p>}<section className="card import-card"><h2>1. Загрузите файл</h2><p>До 5 МБ и 2000 строк. Формулы не выполняются и помечаются ошибкой.</p><form action={uploadImportAction} className="inline-form"><input type="file" name="file" accept=".csv,.xlsx,text/csv,application/vnd.openxmlformats-officedocument.spreadsheetml.sheet" required/><button className="primary">Показать preview</button></form></section></AppShell>}
