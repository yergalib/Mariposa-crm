import{notFound}from"next/navigation";import{AppShell}from"@/components/AppShell";import{OrderForm}from"@/components/OrderForm";import{requireRouteAccess}from"@/lib/auth/session";import{createTenantContext}from"@/lib/tenant/context";import{getOrder,getOrderFormOptions}from"@/lib/orders/queries";import{updateOrderAction}from"../../actions";export default async function Edit({params,searchParams}:{params:Promise<{id:string}>;searchParams:Promise<{error?:string}>}){const s=await requireRouteAccess("/orders"),{id}=await params,p=await searchParams,t=createTenantContext(s.organizationId),[o,opts]=await Promise.all([getOrder(t,id),getOrderFormOptions(t)]);if(!o)notFound();return <AppShell active="/orders" title={`Редактирование ${o.orderNumber}`} subtitle="Изменения активной брони применяются атомарно">{p.error&&<p className="notice error">{p.error}</p>}<OrderForm action={updateOrderAction} options={opts} order={o}/></AppShell>}
